NN DNN NN => CNN RNN => 추론
이런 식으로 간다.
============================================================================================================================================================================
y값에 따라
회귀 아님 분류로 나뉜다.
============================================================================================================================================================================
                회귀                    분류(이진)              분류(다중)               고쳐야 하는 위치
oneHotEncoding   X                        X                         X
------------------------------------------------------------------------------------- #1 데이터
마지막 레이어의
activation      linear(디폴드)       sigmoid =1/1-e^2          softmax
------------------------------------------------------------------------------------- #2 모델 구성
마지막 레이어의
노드의 개수       알아서 주면 (N)       무조건 1개!!!!!!!          y의 클래스 개수
------------------------------------------------------------------------------------- #2 모델 구성
loss            mse,mae,rmse...     binary_crossentropy       categorical_crossentropy 
------------------------------------------------------------------------------------- #3 컴파일, 훈련
predict          무                    np.round()              np.argmax
------------------------------------------------------------------------------------- #4 평가, 예측
============================================================================================================================================================================
###################################################원핫 1. to_categorical############################################
y = to_categorical(y)
print (y)
print (y.shape)
치명적인 !! : 무조건 칼럼 0부터 시작하기 때문에 성능이 떨어질 수 있다.
###################################################원핫 2. in pandas#################################################
y = pd.get_dummies(y, dtype=int)
print(y)
###################################################원핫 3. in sklearn################################################
from sklearn.preprocdssing import oneHotEncoder
ohe = oneHotEncoder()
y =ohe.fit_transform(y)
print(y)
####################################################################################################################
reshape // y = y.reshape(-1, 1) #데이터 전체를 뜻하는 거 -1 ? //데이터의 가장 마지막을 알아야 할 때 -1을 표시하면 가장 끝 값이 나온다. 
1. 내용 (값)
2. 순서
바뀌지 않는다.
[1,2,3] (3,)
이 형태를 
[[1],[2],[3]] (3,1)
이 형태로 바꾸는 것
============================================================================================================================================================================

1, 2, 3
|_____________________ 스칼라 (0차원)

[1, 2, 3] 벡터 (1차원)

[[1,2,3],[4,5,6]] 행렬(매트릭스) (2차원)  - X
[X, X] 텐서 (3차원) - Y

Y, Y, Y 4차원 텐서
============================================================================================================================================================================
