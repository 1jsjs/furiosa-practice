0911에 수정한 파일은 keras/0910 폴더에 있는 keras28_ ~~.py 파일들에 추가한 것 + 0911 폴더에 있는 파일들임
============================================================================================================================================================================
이미지 cnn 에서
min max scaler를 써서 값을 0~1 로 만들거나
standard scaler를 써서 평균을 기준 0을 잡고 <- 0 -> 값을 줄인다.
z = (xᵢ - mean(x)) / stdev(x)

============================================================================================================================================================================
scaler 
1. minmax

2. standard
원 데이터    10      20      30
평균                20
평균 - 원값 -10      0       10
분산        (-10)^2 0^2      10^2 = 200 / 3 = 66.66666666
표준편차    루트 분산  = 8.XXXXXXX
https://jiazzang.tistory.com/3
> 평균 값을 기준을 잡고 0으로 만들어버리겠다

3. MaxAbsScaler (max최대abs절댓값scaler)
                        -100        -50     0       100     200*
max(Abs)로 나누기         -0.5      -0.25    0        0.5     1

4. RobustScaler - 데이터 이상치에 강력한 놈
RobustScaler를 사용하면 모든 변수들이 같은 스케일을 갖게 되며, StandardScaler에 비해 스케일링 결과가 더 넓은 범위로 분포하게 됩니다.
따라서 StandardScaler에 비해 이상치의 영향이 적어진다는 장점이 있습니다.
robust.png 보기 
더 찾아보기
============================================================================================================================================================================
가중치 저장


============================================================================================================================================================================
