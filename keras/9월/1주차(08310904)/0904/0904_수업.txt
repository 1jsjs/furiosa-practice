1. 지표에 대해
MSE = (1/n) * Σ(y_i - ŷ_i)^2

RMSE =mse에 루트를

r2 = 1 - mse/????

=====================================================================================================================================================================================

*추가*
MAE (절댓값) = |y - ŷ|
 ㄴ 쓸 순 있지만 역전파에 미분이 들어가서 mse가 미분할때 더 편함.

##성능향상하는법(하이퍼파라미터 튜닝)
1. EPOCH 횟수 조절
2. 노드의 개수 조절
3. 레이어의 깊이 조절 (딥러닝)
4. 훈련 bathc 사이즈 조절 (전체 데이터를 분할해서)
5. 트레인 사이즈
6. 랜덤사이즈

=====================================================================================================================================================================================


열 컬럼 속성 특성 어트리뷰트 피처

=====================================================================================================================================================================================

train.csv           -> x_train, y_train / x_test, y_test >> 학습, 검증
|    test.csv       | (715,9)
|    submission.csv | (715, )
|__________> model.predict (x_test) > 여기서 나오는 게 ŷ
                               |____________> 여기에 test.csv 넣어서 submission.csv 만들어서 제출


=====================================================================================================================================================================================

*하이퍼파라미터 튜닝

random_state
train_size
레이어의 길이
노드의 갯수
epoch
batch_size

=====================================================================================================================================================================================


이제부터 할 때
내가 어떤 걸 쓸 건지 쓰기 (마음대로 하되, 이런 명세서는 무조건 쓰기)

예
* 1차시도
random_state = 42
train_sie = 0.8
epoch = 50
batch_size = 1
* 결과
rmse :
loss : 
r2   :

=====================================================================================================================================================================================

따릉이에서 헷갈린 이유는 test라는 이름이 두 번 다른 뜻으로 쓰였기 때문
train.csv  ← 정답 count가 있는 원본 데이터
   │
   ├─ x, y로 분리
   │    ├─ x: 문제(9개 날씨·시간 특성)
   │    └─ y: 정답(count)
   │
   └─ 다시 학습용 / 평가용으로 분리
        ├─ x_train, y_train → #3 model.fit()에 사용
        └─ x_val, y_val     → #4 내 모델 성능 평가에 사용

test.csv                 → 정답이 없으므로 평가 불가, 예측만 함
submission.csv           → test.csv 예측값을 담아 제출하는 양식

x_train, x_val, y_train, y_val
대회와 실무 모두 보통 이 원리를 씁니다.
- 정답 있는 과거 데이터 → 학습용과 검증용으로 나눔
- 정답 없는 새 데이터 → 예측
- 실제 정답이 나중에 생기면 → 성능 평가
네가 몰랐던 건 x/y 분리가 아니라, 평가할 정답을 확보하려고 train 데이터에서 한 번 더 나눈다는 단계임

=====================================================================================================================================================================================

alt 노트 링크
https://altalt.io/note/b8ec9780-78a5-4ade-b792-74a20a2206fb

=====================================================================================================================================================================================

count 값에 음수가 나오면 안 되는데 지금 submit_0904_1413.csv 여기 count 칼럼에 음수가 나왔다.
해결 방법!!!!
y =w*x + b 에 하나가 숨어있었다.
layer 층에는 activation (활성화함수 or (layer값)한정 함수) 라는 게 있음
**relu**
0 이상은 그대로 넘기고 0 이하는 0으로 처리되어서 넘긴다.

하지만 음수로 나오는 것을 해결하는 근본적인 해결책은 아니다
그냥 걍 성능이 좋음 함수일뿐
https://sonstory.tistory.com/66
=====================================================================================================================================================================================

