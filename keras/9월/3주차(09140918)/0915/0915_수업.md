https://developer.nvidia.com/

1. Nvidia Driver 설치 최신
https://www.nvidia.com/ko-kr/drivers/details/279738/
GeForce Game Ready 드라이버 616.92 | Windows 10 64-bit
드라이버 홈 > GeForce RTX 4060 Laptop GPU | Windows 10 64-bit > GeForce Game Ready 드라이버

2. Cuda 설치 11.2.2
https://developer.nvidia.com/cuda-11.2.2-download-archive?target_os=Windows&target_arch=x86_64&target_version=10&target_type=exelocal
비주얼스튜디오integration docu, sample / 드라이버, 샘플 컴포넌트 제외

3. Cudnn 설치 8.1
Download cuDNN v8.1.1 (Feburary 26th, 2021), for CUDA 11.0,11.1 and 11.2
https://developer.nvidia.com/rdp/cudnn-archive
4. 가상환경 추가 (tf29x-gpu)
conda create -n tf29x-gpu python=3.10 (5도 같이 함)
5. Python 3.10.x
6. tensorflow 2.9.x
pip install tensorflow-gpu==2.9.3
*A module that was compiled using NumPy 1.x cannot be run in
NumPy 2.2.6 as it may crash. To support both 1.x and 2.x
versions of NumPy, modules must be compiled with NumPy 2.0.
Some module may need to rebuild instead e.g. with 'pybind11>=2.12'.*
    1. 먼저 pip uninstall numpy
    2. 그리고 1.18.5 버전 다시 깔아보기
        1. 1.18.5 안 됨
        2. 1.19.5 안 됨
        3. 1.26.4 된다!!!!!!!
            pip install numpy==1.26.4
    3. import tensorflow as tf
        tf.__version__

6. pip install scikit-learn==1.7.2

========================================================================================================================================================================
자주 쓸 명령어
nvidia-smi
nvcc -V # 쿠다 깔렸냐?????

========================================================================================================================================================================
keras35_gpu_test04 부터 10까지 남은 시간에 해보기
========================================================================================================================================================================
## CNN
DNN         CSV         2차원
RNN                     3차원
CNN                     4차원 (가로, 세로, 칼라)

사람 사진이 있고 0이 남자 1이 여자라고 하면
사진 - x
0과 1 - y

y는 01과 1로 수치화를 했다.
그럼 사진도 수치화를 해야 한다.
사진은 픽셀 화소로 수치화를 한다.
4차원 데이터.

### 행무시 열우선 ###


5x5 흑백 그림이 100000장 있다면
(10000,5,5,1) 

이미지를 사람이 정하는 필터 값에 따라 조각내서 학습시킨다.
커널 사이즈(필터) 값 조절도 > 하이퍼 파라미터 조절!

========================================================================================================================================================================

커널 사이즈 = 가중치
cnn이라는 것은 이미지를 조작조각 내서 조합을 맞추는 걸 반복한다. 높은 값은 높아지고 낮은 값은 낮아지고 해서  x는 y야 이렇게 학습을 시키는 것
레이어는 최수 2개 
ㄴ 한번 해서는 특징이 잘 나오지 않는다.