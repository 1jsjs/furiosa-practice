# 2026-09-22 수업 정리

## 핵심 주제

- `ImageDataGenerator`를 이용한 이미지 데이터 증강
- `numpy` 배열 복사·선택·reshape·concatenate
- 증강 데이터와 원본 데이터 결합
- 데이터 스케일링(`rescale=1./255`)
- 원-핫 인코딩과 라벨 shape
- CNN의 `Flatten`, `GlobalAveragePooling2D`, `Dropout`
- `EarlyStopping`, `ModelCheckpoint`를 이용한 학습 관리

## 오늘의 흐름

1. `keras50_flow2_next.py`
   - 한 장의 이미지를 `np.tile`로 반복해 증강 흐름을 확인
   - `datagen.flow()`의 반환값과 배열 shape를 확인하는 기초 실습
2. `keras51_augment1_fashion.py`
   - Fashion-MNIST에서 원본 60,000개 중 40,000개를 랜덤 선택
   - 증강 후 원본과 합쳐 약 100,000개 학습 데이터 구성
3. `keras51_augment2_mnist.py`
   - MNIST에 같은 증강 과정을 적용
   - `(N, 28, 28)` → `(N, 28, 28, 1)`로 reshape
4. `keras51_augment3_cifar10.py`
   - CIFAR-10 RGB 데이터 `(N, 32, 32, 3)` 증강
   - `GlobalAveragePooling2D`를 사용해 `Flatten` 대신 채널별 평균으로 축소
5. `keras51_augment4_cifar100.py`
   - CIFAR-100 RGB 데이터에 증강과 `rescale` 적용
   - CIFAR-10과 동일한 CNN 흐름을 CIFAR-100에 확장
6. `keras51_augment5_men_women_onlywomen.py`
   - 저장된 `.npy` 데이터를 불러와 여성 라벨만 선택
   - 선택된 여성 데이터만 추가 증강한 뒤 기존 데이터와 결합

## shape·라벨 정리

| 단계 | Fashion/MNIST | CIFAR-10/100 | 남녀 데이터 |
|---|---|---|---|
| 입력 이미지 | `(N, 28, 28, 1)` | `(N, 32, 32, 3)` | `(N, 300, 300, 3)` |
| 증강 방식 | 이동·회전·확대·반전 등 | 동일 계열의 이미지 변환 | 여성 샘플만 선택 후 변환 |
| 출력 클래스 | 10 | CIFAR-10은 10, CIFAR-100은 100 | 2 |
| 대표 loss | `categorical_crossentropy` | `sparse_categorical_crossentropy` | `categorical_crossentropy` |

## 질문·중요·실습

### [질문]

- `np.tile`이 배열을 어떤 shape로 반복하는지
- `datagen.flow()`가 반환하는 값과 실제 이미지 배열의 shape
- `reshape(-1, 28, 28, 1)`에서 `-1`이 의미하는 것
- 원본과 증강 데이터를 `np.concatenate`할 때 이미지와 라벨의 순서를 맞추는 방법

### [중요]

- 스케일러와 `rescale`의 차이
- 원-핫 인코딩 전후의 라벨 shape
- `OneHotEncoder`는 train에 `fit`하고 test에는 `transform`하는 원칙
- 이미지 데이터는 모델 입력 전에 채널 축을 확인해야 함
- 증강 데이터와 원본 데이터의 값 범위가 동일해야 함

### [실습]

- 0918·0921·0922 실습은 저장한 남녀 데이터와 CNN 학습 코드까지 연결됨
- 데이터 증강 전후의 정확도와 과적합 여부 비교
- Fashion-MNIST, MNIST, CIFAR-10, CIFAR-100, 남녀 데이터의 결과 비교

## 다음 실행 전 확인할 것

- `keras51_augment1_fashion.py`, `keras51_augment2_mnist.py`: test 라벨은 `ohe.fit_transform()`이 아니라 train에 맞춘 `ohe.transform()` 사용 여부 확인
- `keras51_augment4_cifar100.py`: CIFAR-100은 100개 클래스이므로 마지막 `Dense` 출력과 loss의 라벨 형식 확인
- `keras51_augment5_men_women_onlywomen.py`: 여성 필터링은 원-핫 라벨의 두 번째 열 기준으로 한 번만 수행하고, 이후 배열을 다시 덮어쓰지 않도록 정리
- 모든 실습에서 `x_train`, `y_train`, `x_test`, `y_test`의 shape와 값 범위를 출력해 확인
- 정확도 비교 시 `model.evaluate()` 결과와 `model.predict()` 기반 accuracy를 섞지 않기

## 오늘 Git 기록

- `9ea81e0` 13:39 — `중간점검`: 증강 실습 1·2와 CIFAR 실습 뼈대 추가
- `336e7f9` 17:18 — `9월 22일 끝`: Fashion/MNIST/CIFAR/남녀 증강 코드 연결 및 보완
- `259a84c` 17:23 — `9/22 추가`: 수업 로그와 필기 이미지 반영

## 한 줄 요약

오늘은 원본 이미지에서 랜덤 샘플을 뽑아 증강하고, shape·스케일·라벨 형식을 맞춘 뒤 CNN 학습 데이터로 합치는 전체 흐름을 연습했다.
