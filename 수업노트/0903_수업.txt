train 과 test 데이터를 나누는 이유
ㄴ 명확한 판단을 하기 위해

1. 데이터 전체를 그냥 비율로 나누기
2. 데이터를 랜덤하게 나누기
3. sklearn 자르기 (train test split)

            |같은 개념
            | loss
mean squared| error   -> MSE = (1/n) * Σ(y_i - ŷ_i)^2
|           | 오차
|           | cost(비용)
|
|
|
|____> MSE의 문제점 : 제곱을 한다면 값이 너무 커진다
            |_______>루트를 씌워서 해결하자
                     Root MSE = RMSE

R2(R-squared (r2 score)) : https://go-hard.tistory.com/125 // 음수가 나올 수 있다.

AI 모델링이 2개 있다.
1. 회귀 모델 (수치로 나타내는 거) : loss - 주지표 , R2 - 보조지표 // 수치 하나로만 평가하기에는 상대적이라 R2 라는 지표를 써서 성능을 측정한다.
2. 분류 모델 (라벨링해서 맞추면 되는 거) : 정확도는 보조지표로 쓴다. 

기준은 **loss**

RMSE R2 